import { useTranslations } from 'next-intl';
import { Link } from '@/i18n/routing';
import { footerNav } from '@/config/nav';
import { GitHubRepositoryLink } from './GitHubRepositoryLink';
import { LocaleSwitcher } from './LocaleSwitcher';
import { BrandMark } from '@/components/brand/BrandMark';
export function Footer() { const t = useTranslations(); return <footer className="border-t border-black/5 bg-slate-50 dark:border-white/5 dark:bg-slate-950"><div className="mx-auto max-w-7xl px-4 py-14 sm:px-6 lg:px-8"><div className="grid gap-10 sm:grid-cols-2 lg:grid-cols-5"><div className="sm:col-span-2"><Link href="/" className="inline-flex items-center gap-2.5"><BrandMark className="h-[30px] w-[30px] text-slate-700 dark:text-[#d9e0ea]" /><span className="font-heading text-sm font-bold tracking-[.2em]">CLIPSX</span></Link><p className="mt-4 max-w-sm text-sm leading-6 text-slate-600 dark:text-slate-400">{t('Footer.tagline')}</p><GitHubRepositoryLink className="focus-ring mt-5 inline-flex items-center gap-2 rounded-lg text-sm text-slate-600 transition hover:text-violet-600 dark:text-slate-400 dark:hover:text-violet-300" label={t('Footer.github')}/></div>{Object.entries(footerNav).map(([group, links]) => <div key={group}><p className="eyebrow mb-4">{t(`Footer.${group}` as never)}</p><ul className="space-y-3">{links.map((item) => <li key={item.href}>{'external' in item && item.external ? <a href={item.href} target="_blank" rel="noreferrer" className="text-sm text-slate-600 hover:text-slate-950 dark:text-slate-400 dark:hover:text-white">{t(item.labelKey as never)}</a> : <Link href={item.href} className="text-sm text-slate-600 hover:text-slate-950 dark:text-slate-400 dark:hover:text-white">{t(item.labelKey as never)}</Link>}</li>)}</ul></div>)}</div><div className="mt-12 flex flex-col gap-4 border-t border-black/5 pt-6 text-xs text-slate-500 sm:flex-row sm:items-center sm:justify-between dark:border-white/5"><p>© {new Date().getFullYear()} ClipsX. {t('Footer.rights')}</p><div className="flex flex-wrap items-center gap-3"><p>{t('Footer.localPromise')}</p><LocaleSwitcher /></div></div></div></footer>; }
