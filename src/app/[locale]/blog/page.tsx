import type { Metadata } from 'next'; import { ArrowRight, Clock3 } from 'lucide-react'; import { setRequestLocale } from 'next-intl/server'; import { Link } from '@/i18n/routing'; import { PageIntro } from '@/components/marketing/Marketing'; import { blogPosts } from '@/content/blog'; import { pick } from '@/content/marketing'; import type { Locale } from '@/i18n/config';
export const metadata:Metadata={title:'Blog',description:'Notes on programmable clipboard workflows, privacy, local intelligence, and extensions.'};
export default async function Blog({params}:{params:Promise<{locale:Locale}>}){const {locale}=await params;setRequestLocale(locale);return <div className="marketing-shell pb-24"><PageIntro eyebrow={locale==='ja'?'ブログ':'Field notes'} title={locale==='ja'?'クリップボードを、よく考える。':'Thinking at the clipboard boundary.'} description={locale==='ja'?'プライバシー、ローカル AI、拡張可能なワークフローについて。':'Practical notes on privacy, local intelligence, and programmable workflows.'}/><div className="grid gap-5 md:grid-cols-3">{blogPosts.map(post=><article key={post.slug} className="marketing-card flex flex-col p-6"><div className="flex items-center gap-2 text-xs text-slate-500"><time dateTime={post.date}>{post.date}</time><span>·</span><Clock3 size={13}/><span>{post.readMinutes} min</span></div><h2 className="mt-6 font-heading text-xl font-bold text-slate-950 dark:text-white">{pick(post.title,locale)}</h2><p className="mt-3 flex-1 text-sm leading-6 text-slate-600 dark:text-slate-400">{pick(post.description,locale)}</p><Link href={`/blog/${post.slug}`} className="mt-6 inline-flex items-center text-sm font-bold text-violet-700 dark:text-violet-300">{locale==='ja'?'読む':'Read article'}<ArrowRight className="ml-2" size={15}/></Link></article>)}</div></div>}
